import { useState } from "react";
import "./App.scss";
import { Button } from "@mui/joy";

function App() {
  const [count, setCount] = useState(0);

  return (
    <div>
      <h3>好耶</h3>
      <Button>aa</Button>
    </div>
  );
}

export default App;
